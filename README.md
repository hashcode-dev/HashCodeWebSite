# Run and deploy your app

This contains everything you need to run your app locally.

View your app in AI Studio: https://ai.studio/apps/fa256933-7872-492c-a6b9-71299af83982

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.example) to your Gemini API key
3. Run the app:
   `npm run dev`
